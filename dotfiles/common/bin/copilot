#!/usr/bin/env bash
set -euo pipefail

exec env \
    -u COLORTERM \
    COPILOT_ALLOW_ALL=true \
    FORCE_COLOR=1 \
    TERM=xterm-256color \
    "$HOME/.local/bin/copilot" \
    --allow-all \
    "$@"

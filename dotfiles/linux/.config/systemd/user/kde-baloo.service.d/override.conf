[Service]
# The CachyOS service references a KDE-only condition helper that is not
# available in this Hyprland session.
ExecCondition=
# The metadata extractor creates a Qt application but this user service has no
# display environment, so force Qt's non-interactive platform.
Environment=QT_QPA_PLATFORM=offscreen
